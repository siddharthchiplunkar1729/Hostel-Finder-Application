# Database (used by docker-compose and local app)
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=hostelhub

# Local app connection (outside Docker)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/hostelhub?sslmode=disable

# Auth
JWT_SECRET=change-this-to-a-long-random-secret

# App
NODE_ENV=development
NEXT_PUBLIC_API_URL=http://localhost:3000/api
